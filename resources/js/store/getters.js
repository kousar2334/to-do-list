export default {
    isUserLogin(state) {
        return state.isUserLogin;
    },

    authUser(state) {
        return state.authUser;
    },

    userToken(state) {
        return state.userToken;
    },
};
